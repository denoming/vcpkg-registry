The package mosquitto provides CMake targets:

    find_package(mosquitto REQUIRED)
    target_link_libraries(main PRIVATE mosquitto::mosquitto)

and optionally:

    target_link_libraries(main PRIVATE mosquitto::mosquittopp)
